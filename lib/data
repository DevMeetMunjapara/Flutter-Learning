Column(
          children: transaction.map((e) {
            return Card(
              child: Row(
                children: <Widget>[
                  Text(e.id),
                  Text(e.title),
                  Text(e.amount.toString()),
                  Text(e.date.toString()),
                ],
              ),
            );
          }).toList(),
        )